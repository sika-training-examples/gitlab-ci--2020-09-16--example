deploy:
  script: echo 'Deploy!'
  environment:
    name: dev/$CI_COMMIT_REF_SLUG
    url: https://$CI_COMMIT_REF_SLUG.dev.company.com

deploy prod:
  script: echo 'Deploy!'
  when: manual
  environment:
    name: prod
    url: https://app1.prod.company.com
